import { LucideIcon, Minus, TrendingDown, TrendingUp } from 'lucide-react'; import { cn } from '@/lib/cn';
interface Props{label:string;value:string|number;icon?:LucideIcon;trend?:{value:string;direction:'up'|'down'|'flat'};hint?:string;className?:string}
export function StatCard({label,value,icon:Icon,trend,hint,className}:Props){return <div className={cn('rounded-xl border border-border-subtle bg-bg-secondary p-5 shadow-card transition hover:border-border-default',className)}><div className="mb-3 flex items-center justify-between"><span className="label-section">{label}</span>{Icon&&<div className="flex h-7 w-7 items-center justify-center rounded-md bg-bg-tertiary text-text-tertiary"><Icon size={14}/></div>}</div><div className="text-mono mb-2 text-[26px] font-semibold leading-none">{value}</div>{trend&&<TrendIndicator {...trend}/>} {hint&&<p className="mt-1 text-[13px] font-light italic text-text-secondary">{hint}</p>}</div>}
function TrendIndicator({value,direction}:{value:string;direction:'up'|'down'|'flat'}){const Icon=direction==='up'?TrendingUp:direction==='down'?TrendingDown:Minus; return <div className={cn('flex items-center gap-1 text-[14px] font-medium',direction==='up'?'text-success':direction==='down'?'text-danger':'text-text-tertiary')}><Icon size={12} strokeWidth={2.5}/>{value}</div>}
