import { forwardRef, ButtonHTMLAttributes } from 'react'; import { LucideIcon } from 'lucide-react'; import { cn } from '@/lib/cn';
interface Props extends ButtonHTMLAttributes<HTMLButtonElement>{icon:LucideIcon;hasDot?:boolean;size?:'sm'|'md';active?:boolean}
export const IconButton=forwardRef<HTMLButtonElement,Props>(function IconButton({icon:Icon,hasDot,size='md',active,className,...props},ref){return <button ref={ref} className={cn('relative flex items-center justify-center rounded-md border border-transparent text-text-secondary transition hover:border-border-subtle hover:bg-bg-tertiary hover:text-text-primary disabled:opacity-40',size==='md'?'h-8 w-8':'h-7 w-7',active&&'bg-accent-subtle text-accent',className)} {...props}><Icon size={size==='md'?14:13} strokeWidth={2}/>{hasDot&&<span className="absolute right-1 top-1 h-1.5 w-1.5 animate-pulse-dot rounded-full bg-danger"/>}</button>});
