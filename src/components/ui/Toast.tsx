import { AlertTriangle, CheckCircle, Info, X, XCircle } from 'lucide-react'; import { useToastStore, type ToastKind } from '@/stores/toastStore';
const ICONS:Record<ToastKind,typeof CheckCircle>={success:CheckCircle,error:XCircle,info:Info,warning:AlertTriangle}; const COLORS:Record<ToastKind,string>={success:'border-success/30 text-success',error:'border-danger/30 text-danger',info:'border-info/30 text-info',warning:'border-warning/30 text-warning'};
export function ToastContainer(){const {toasts,dismiss}=useToastStore();return <div className="fixed bottom-6 right-6 z-[100] flex w-80 flex-col gap-2">{toasts.map(t=>{const Icon=ICONS[t.kind];return <div key={t.id} className={'flex items-start gap-2.5 rounded-lg border bg-bg-elevated p-3 shadow-modal animate-slide-up '+COLORS[t.kind]}><Icon size={16} className="mt-0.5 flex-shrink-0"/><p className="flex-1 text-[14px] text-text-primary">{t.message}</p><button onClick={()=>dismiss(t.id)} className="text-text-tertiary hover:text-text-primary"><X size={14}/></button></div>})}</div>}
