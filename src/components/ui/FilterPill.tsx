import { ButtonHTMLAttributes } from 'react'; import { cn } from '@/lib/cn';
interface Props extends ButtonHTMLAttributes<HTMLButtonElement>{label:string;count?:number;active?:boolean}
export function FilterPill({label,count,active,className,...props}:Props){return <button className={cn('whitespace-nowrap rounded-full px-3 py-1.5 text-[12px] font-medium transition',active?'border border-accent/30 bg-accent-subtle text-accent':'border border-border-subtle bg-bg-tertiary text-text-secondary hover:bg-bg-elevated hover:text-text-primary',className)} {...props}>{label}{count!=null&&<span className="ml-1.5 opacity-60">· {count}</span>}</button>}
