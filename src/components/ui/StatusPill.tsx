import { cn } from '@/lib/cn';
type Status='active'|'paused'|'draft'|'archived'|'live'|'scheduled'|'finished'|'error'; const C:Record<Status,{color:string;label:string}>={active:{color:'bg-success/15 text-success',label:'activa'},paused:{color:'bg-warning/15 text-warning',label:'pausada'},draft:{color:'bg-text-tertiary/15 text-text-tertiary',label:'borrador'},archived:{color:'bg-text-disabled/15 text-text-disabled',label:'archivada'},live:{color:'bg-success/15 text-success',label:'en vivo'},scheduled:{color:'bg-info/15 text-info',label:'programado'},finished:{color:'bg-purple/15 text-purple',label:'finalizado'},error:{color:'bg-danger/15 text-danger',label:'error'}};
export function StatusPill({status,label,pulsing=status==='live',className}:{status:Status;label?:string;pulsing?:boolean;className?:string}){const c=C[status];return <span className={cn('inline-flex items-center gap-1.5 rounded-full px-2 py-0.5 text-[11px] font-medium',c.color,className)}><span className={cn('h-1.5 w-1.5 rounded-full bg-current',pulsing&&'animate-pulse-dot')}/>{label??c.label}</span>}
