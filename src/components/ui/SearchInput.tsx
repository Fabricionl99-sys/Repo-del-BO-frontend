import { InputHTMLAttributes, forwardRef } from 'react'; import { Search } from 'lucide-react'; import { cn } from '@/lib/cn';
interface Props extends InputHTMLAttributes<HTMLInputElement>{containerClassName?:string}
export const SearchInput=forwardRef<HTMLInputElement,Props>(function SearchInput({containerClassName,className,placeholder='Buscar...',...props},ref){return <div className={cn('relative flex items-center',containerClassName)}><Search size={14} className="pointer-events-none absolute left-3 text-text-tertiary"/><input ref={ref} type="text" placeholder={placeholder} className={cn('w-full rounded-lg border border-border-default bg-bg-tertiary py-2 pl-9 pr-3 text-[15px] font-medium text-text-primary placeholder:text-text-tertiary transition focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent-subtle',className)} {...props}/></div>});
