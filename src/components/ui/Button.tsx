import { forwardRef, ButtonHTMLAttributes, ReactNode } from 'react';
import { cn } from '@/lib/cn';
type Variant='primary'|'secondary'|'ghost'|'danger'; type Size='sm'|'md';
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement>{variant?:Variant;size?:Size;icon?:ReactNode;iconPosition?:'left'|'right';loading?:boolean}
const V:Record<Variant,string>={primary:'bg-accent text-bg-primary hover:bg-accent-hover font-semibold',secondary:'bg-bg-tertiary text-text-primary border border-border-default hover:bg-bg-elevated hover:border-border-strong',ghost:'text-text-secondary hover:bg-bg-tertiary hover:text-text-primary',danger:'bg-danger/15 text-danger border border-danger/25 hover:bg-danger/20'};
const S:Record<Size,string>={sm:'px-2.5 py-1 text-[12px] rounded-md gap-1.5',md:'px-3.5 py-2 text-[13px] rounded-lg gap-2'};
export const Button=forwardRef<HTMLButtonElement,ButtonProps>(function Button({variant='secondary',size='md',icon,iconPosition='left',loading,children,className,disabled,...props},ref){return <button ref={ref} className={cn('inline-flex items-center justify-center font-medium transition disabled:cursor-not-allowed disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg-primary',V[variant],S[size],className)} disabled={disabled||loading}{...props}>{loading?<span aria-label="procesando" className="inline-block h-3.5 w-3.5 animate-spin rounded-full border-2 border-current border-r-transparent"/>:<>{icon&&iconPosition==='left'&&icon}{children}{icon&&iconPosition==='right'&&icon}</>}</button>});
