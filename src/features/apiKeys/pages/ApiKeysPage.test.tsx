import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { fireEvent, render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import ApiKeysPage from './ApiKeysPage';
function wrap(ui:React.ReactNode){return render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}>{ui}</QueryClientProvider>)}
describe('ApiKeysPage',()=>{it('renderiza credenciales y recursos',async()=>{wrap(<ApiKeysPage/>); expect(await screen.findByText(/credenciales de producción/i)).toBeInTheDocument(); expect(screen.getByText(/consumo y límites/i)).toBeInTheDocument(); fireEvent.click(screen.getByText('sandbox')); expect(await screen.findByText(/credenciales de sandbox/i)).toBeInTheDocument(); fireEvent.click(screen.getByText('webhooks')); expect(screen.getByText('Webhooks')).toBeInTheDocument(); fireEvent.click(screen.getByText('logs de requests')); expect((await screen.findAllByText('/v1/events')).length).toBeGreaterThan(0)})});
