import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { fireEvent, render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import TeamPage from './TeamPage';
function wrap(ui:React.ReactNode){return render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}>{ui}</QueryClientProvider>)}
describe('TeamPage',()=>{it('renderiza tabla de miembros desde MSW',async()=>{wrap(<TeamPage/>); expect(await screen.findByText('Fabricio Lasagna')).toBeInTheDocument(); expect(screen.getByText('Invitar miembro')).toBeInTheDocument(); fireEvent.click(screen.getByText('Roles y permisos')); expect(screen.getByText('4 roles fijos disponibles')).toBeInTheDocument(); fireEvent.click(screen.getByText('Historial de acceso')); expect(screen.getByText('Fabricio inició sesión desde Lima')).toBeInTheDocument(); fireEvent.click(screen.getByText('Miembros')); fireEvent.change(screen.getByPlaceholderText('buscar por nombre, email o rol...'),{target:{value:'maria'}}); expect(await screen.findByText('María López')).toBeInTheDocument()})});
