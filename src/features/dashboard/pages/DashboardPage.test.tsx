import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { describe, expect, it } from 'vitest'; import DashboardPage from './DashboardPage';
function wrap(ui:React.ReactNode){return render(<MemoryRouter><QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}>{ui}</QueryClientProvider></MemoryRouter>)}
describe('DashboardPage',()=>{it('muestra KPIs y acciones rápidas',async()=>{wrap(<DashboardPage/>); expect(await screen.findByText('jugadores activos')).toBeInTheDocument(); expect(screen.getByText('Crear regla de XP')).toBeInTheDocument()})});
