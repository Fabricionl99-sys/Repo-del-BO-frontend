import { Construction, Mail } from 'lucide-react'; import { Button } from '@/components/ui/Button'; import { PageHeader } from '@/components/ui/PageHeader';
export default function ComingSoonPage({title='Próximamente',description='esta sección está en desarrollo'}:{title?:string;description?:string}){return <><PageHeader title={title} subtitle={description}/><div className="flex flex-col items-center justify-center py-20 text-center"><div className="mb-5 flex h-20 w-20 items-center justify-center rounded-full bg-bg-tertiary"><Construction size={36} className="text-text-tertiary" strokeWidth={1.5}/></div><h3 className="mb-2 text-[16px] font-bold">próximamente</h3><p className="mb-6 max-w-md text-[15px] text-text-tertiary">esta sección está en construcción dentro del roadmap del BO</p><Button icon={<Mail size={14}/>} onClick={()=>window.open('mailto:soporte@niveles.io')}>contactar soporte</Button></div></>}
